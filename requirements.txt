redis
prometheus-client
aiohttp
pydantic
httpx
fastapi>=0.100.0
uvicorn>=0.20.0
watchdog>=2.1.0
aiofiles>=0.8.0
websockets>=10.0
aiohttp_retry
PyYAML>=6.0
requests>=2.28.0
psutil>=5.9.0
python-dotenv>=1.0.0


beautifulsoup4>=4.11.0
Pillow>=9.0.0

# 注意：以下依赖已移至具体的MCP服务器或运行时模块中
# - matplotlib, pandas, numpy, scikit-learn -> runtimes/reasoning/requirements.txt
# - playwright -> MCP服务器自行管理
# - selenium, webdriver-manager -> 已删除，不再使用